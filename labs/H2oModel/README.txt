Running the H2o Model in Fusion.

Unzip the Model Jar and place in into Fusion's  WEB-INF/lib folder of connectors and api
-This is an uber jar that contains a pretrained Gradient Boosting Model on the 20news Dataset.

Then run setup.sh and the class of the document would be in the newsgroup_final label.
